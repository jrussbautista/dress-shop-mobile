import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    padding: 15
  },
  heading: {
    fontSize: 20,
    fontWeight: "700",
    textTransform: "uppercase"
  }
});
